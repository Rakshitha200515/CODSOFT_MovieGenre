This is the movie Genre application ...
this model eventually categories the Genre of the input even with the percentages of audience 
the dataset was preiorly given (Kaggle)
we need to do all this by using venu 
Here the files are frontend + backend +trained model 
index.html file play the role of frontend
for backend i have used flask
app.py file contents backend 
ml_model.py contents all the model information
trained_model contents all the trained model as name specifies 
after training the model we would get the two files named label_encoder.pkl and movie_genere_model.pkl
The screenshot of the projects is been shown below 
